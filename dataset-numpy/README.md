# Dataset Numpy

This folder contains data exports from the notebooks as intermediate steps so data can be passed around between notebooks. Files are `CSV` files with the extracted features from the images (both raw and cleaned).

These files are generated.
